# FILE CONVERSION SKILL
# Goal: PDF -> MD, MD -> TXT

import os
from pathlib import Path

def pdf_to_md(input_path, output_path):
    # Template for PDF extraction logic
    print(f"Converting {input_path} to Markdown...")
    return True

def md_to_txt(input_path, output_path):
    # Template for MD cleanup
    print(f"Converting {input_path} to Text...")
    return True

if __name__ == "__main__":
    print("File Conversion Skill Loaded.")
